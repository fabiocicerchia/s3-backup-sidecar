# Backstage catalog entry, read by paved's scorecard plugin.
#
# Every check paved scores is derived from this file alone — no cluster
# access, no CI credentials. What is absent here is absent from the score.
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: s3-backup-sidecar
  description: >-
      A restic-based backup sidecar with cron built in and 100% env-driven config. Mount your data volume,
      set four env vars, get scheduled, encrypted, deduplicated, retention-managed backups to S3 (or…
  annotations:
    github.com/project-slug: fabiocicerchia/s3-backup-sidecar
    backstage.io/source-location: url:https://github.com/fabiocicerchia/s3-backup-sidecar/
    backstage.io/techdocs-ref: dir:.
  links:
    - url: https://github.com/fabiocicerchia/s3-backup-sidecar
      title: Source
spec:
  type: library
  lifecycle: production
  owner: group:default/kodebeat
  system: oss-tools
